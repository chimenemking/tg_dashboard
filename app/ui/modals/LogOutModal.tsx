import React from 'react'
// Thss modal should be the logout modal component that is displayed when a user clicks the logoutbutton on the dashnboard
// user should get a prompt "Are you sure you want to logout and two options Yes or No, Yes proceeds to logout and back to the login page , No closes the modal"
type Props = {}

const LogOutModal = (props: Props) => {
  return (
    <div>LogOutModal</div>
  )
}

export default LogOutModal